import { ADD_PRODUCT } from "./action";

export const AddTest = text => {
  return { type: ADD_PRODUCT, text };
};

//listes des actions (functions) pour realiser l'application
//Ajouter des elements
//Supprimer des elements
